[user]
	name = Xavier León
	email = xleon.mail@gmail.com
    signingkey = 1C8BF8A7
[credential]
	helper = cache --timeout=3600
[core]
	editor = vim
[color]
	ui = true
[alias]
    st = status -sb
    ci = commit
    br = branch -v
    co = checkout
    df = diff
	lg = log --pretty=format:'%Cgreen%H%Creset - %Cred%an%Creset : %s'
    sl = shortlog --pretty=format:'%Cgreen%h%Creset - %Cred%an%Creset : %s'
    standup = lg ––since yesterday
    purr = pull --rebase
    ready = rebase -i @{u}
    fixup = commit --amend --no-edit
[grep]
    lineNumber = True     ; show line number by default in git grep
    extendedRegex = True  ; --extended-regex by default
[push]
	default = simple
